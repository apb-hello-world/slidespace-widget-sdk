# RSS reader

This declarative Widget API v3 sample accepts an arbitrary RSS or Atom URL. SlideSpace asks the user to approve the feed origin, fetches and parses the XML itself, and renders the repeated article rows without executing widget code.

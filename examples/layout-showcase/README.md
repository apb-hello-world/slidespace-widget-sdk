# Layout showcase

This sample exercises the current Widget API v2 renderer with nested stacks, host bindings, literal text, a spacer, a progress indicator, transparency, and a larger card layout.

It intentionally contains no HTML or JavaScript. The current SlideSpace widget runtime renders reviewed JSON layouts and does not expose arbitrary network requests.
